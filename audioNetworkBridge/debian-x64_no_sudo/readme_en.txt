First：
Run sudo apt-get install libasound2-dev


Method 1
1. SSH transfers audioNetworkBridge all files to any directory in the Raspberry PI.
2. Run 
   sudo chmod a+x./install.sh 
   on the terminal
3. Run 
   sudo ./install.sh 
   on the terminal